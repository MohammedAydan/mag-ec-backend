# Chat Context - 2026-05-25 17:34:06

## Current Repo State

- Branch: `main`
- Active feature: `phase-06-inventory-reservations`
- Worktree goal for this handoff: commit the completed phase-04 and phase-05 work, then resume phase 06 in the next session
- Unrelated files intentionally left untouched:
  - `.antigravitycli/`
  - `GEMINI.md`

## What Was Completed

- Phase 04 is complete:
  - Variant-level pricing persistence
  - Store pricing behavior settings
  - Manual tax classes and country rates
  - Shipping zones and methods
  - Checkout preview endpoint
  - Pricing docs, tests, and migration artifact
- Phase 05 is complete:
  - Promotion, coupon, exclusion, and usage-ledger persistence
  - Admin promotion and coupon upsert APIs
  - Centralized coupon normalization
  - Deterministic eligibility evaluation for fixed, percentage, and free-shipping rewards
  - Checkout-preview integration returning applied and rejected promotions plus discounted subtotals
  - Promotions docs, tests, and migration artifact

## Files and Areas Added

- `apps/api/src/modules/pricing/**`
- `apps/api/src/modules/promotions/**`
- `apps/api/test/e2e/pricing-admin.e2e-spec.ts`
- `apps/api/test/e2e/promotions-admin.e2e-spec.ts`
- `apps/api/test/integration/pricing.integration.spec.ts`
- `apps/api/test/integration/promotions.integration.spec.ts`
- `prisma/migrations/20260525_04_pricing_store_settings/**`
- `prisma/migrations/20260525_05_promotions_coupons/**`
- `docs/domain/pricing-engine.md`
- `docs/domain/promotions-rules.md`

## Verification Status

Passed:

- `pnpm.cmd prisma:generate`
- `pnpm.cmd prisma:validate`
- `pnpm.cmd lint`
- `pnpm.cmd typecheck`
- `pnpm.cmd test`
- `pnpm.cmd test:e2e`
- `pnpm.cmd test:integration`
- `pnpm.cmd build`
- `pnpm.cmd openapi:generate`

Expected non-green:

- `pnpm.cmd openapi:check`
  - Fails only because `packages/contracts/openapi/openapi.json` intentionally differs from `HEAD` after the phase-04 and phase-05 endpoint additions

## Important Implementation Notes

- `PromotionsModule` provides `MoneyService` directly to avoid a circular dependency on `PricingModule`
- Promotion usage is modeled with aggregate counters plus reservation-ready ledger tables, but no claim or consume writes happen yet
- MySQL-gated integration suites remain environment-dependent and skip locally unless explicitly enabled with reachable MySQL

## Exactly What To Do Next

1. Read `plans/context.md`, `plans/SESSION_LOG.md`, and `plans/phase-06-inventory-reservations/*`.
2. Report the phase-06 resume summary.
3. Stay strictly inside phase 06.
4. Start with inventory and reservation persistence design, then build the service and admin API layers.
5. Do not reopen phase 04 or 05 except for incidental compatibility fixes required by phase 06.
